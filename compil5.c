#include <stdio.h>

int main() {
    char c = 'A';
    printf("c = %s\n", c);
    return 0;
}

//Aucun erreur de compilation dans ce fichier.
//Le code déclare une variable de type char avec un seul caractère, lui assigne une valeur, puis affiche une chaine de caractère avec plusieurs caractères.
