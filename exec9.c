#include <stdio.h>

int main() {
    int a = 10;
    int b = 20;
    printf("a=%d b=%d\n",a , b); 
    return 0;
}

//Il n'y a pas de message d'erreur de compilation dans ce fichier.
// La variable 'a' est déclarée mais jamais utilisée, ce qui peut indiquer une erreur logique dans le code.